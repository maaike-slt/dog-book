import type React from "react"
import "./StatBtn.css"

const StatBtn: React.FC = () => {
	return (
		<button
			type="button"
			className="stat-button"
			// onClick={onClick}
		/>
	)
}

export default StatBtn
